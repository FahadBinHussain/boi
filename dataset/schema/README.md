# Schema

JSON Schema files for the main dataset records and generated candidate view.

The validator in `../scripts/validate-dataset.js` does not require external packages; these schema files document the public contract and can later be wired to a stricter validator like Ajv if needed.
